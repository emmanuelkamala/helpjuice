<%= form_with(model: search_analytic) do |form| %>
  <% if search_analytic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(search_analytic.errors.count, "error") %> prohibited this search_analytic from being saved:</h2>

      <ul>
        <% search_analytic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
